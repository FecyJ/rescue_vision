# 20260913 1818 实机诊断与修复

依据 `logs/match_20260913_1818.log` 和 `屏幕录制 2026-09-13 182821.mp4`。
录像中的 `process_t` 比视频进度约小 1～2 秒，以下引用日志进程时间。
当前工作区包含尚未收尾的 2315 重构；本次在现状上修复，不把旧方案的全部验收标为完成。

## 四个问题

| 现象 | 实际证据与原因 | 本次修复 |
| --- | --- | --- |
| 0:40 前长时间等待、空场旋转 | 日志第 37～108 行多次已确认 `1/1`，仍在对准等待后 `confirmation_timeout`。执行器没有结果时反复改写转向完成时间；正式装配又把“当前准备结果无法做位姿补偿”混成“没有 IMU”，使已有转向退回 400 ms 定时器。第 167～232 行随后反复扫描/重选。 | 转向完成时间仅记录一次；已经完成的转向不因微小回弹重新执行。规划结果缺失时仍持续传入已有 IMU 航向，用真实转角完成冻结动作。 |
| 0:41～0:44、1:37～1:40 启动右偏 | 第 305 行开爪航向约 -1.703 rad，第 307 行前进时约 -1.754 rad，右偏约 2.9°，左右目标轮速始终相同。橙色抓取同样进入 `forward_open_loop`。 | 抓取提交时冻结 IMU 航向，开爪伴随前进及编码器前进均反馈纠偏；减速时同比减小纠偏，避免临近终点变成原地转动。新图不能重置此航向或重新选组。 |
| 3:19 附近放弃视野目标 | 第 1157 行局部任务以 `no_safe_grasp_or_recovery` 退出。该帧仍有绿色，但第 1151～1152 行候选 K0 为 455.82/473.52 mm，包含 `outside_near_field`；其他成员有橙色隔离/蓝色阻挡。不能据画面把这些都当作已验证可立即夹取。旧退出路径直接扫描，没有先提交另一个已通过接近检查的目标。 | 先记录失败物理目标，再按现有完整入口检查选择当前视野中的合法替代目标并接近；确实无合法候选时才扫描。危险、隔离、场界和失败区域检查保留。 |
| 5:39 后 search/relocate 循环 | 第 1983 行把 prior `(757.6,22.7)` mm 交给近场，配置近场半径为 450 mm；第 1986 行稳定场景中的绿块 K0 仍为 621.84 mm，第 1994 行却按缺场景退出。第 2032～2057 行重复 `outside_near_field` 后搜索。 | 近场交接统一检查当前距离：远场目标通过路径检查后继续接近，不启动近场会话；搜索中的恢复入口只接管实际在近场的目标，不能因别处存在阻挡就让远场目标停车等待近场计划。 |

全日志的 `grasp_task_failure` 中，`scene_evidence_unavailable` 29 次、`confirmation_timeout` 11 次，
说明这两类退出是主要的软件空转来源。它们不是 40 次真实抓取失败的接触证据。

核查澄清：`measure_target_envelope()` 的 `center` 已使用可靠 K0；HSV 上表面投影只贡献包络边界。
没有改动 K0/HSV 几何权威，也没有扩大近场距离、观测年龄或超时来掩盖交接错误。
现有当前朝向可抓优先排序与完整危险扫掠继续使用。

## 契约和迁移

- 正式 match、NB 和复用正式抓取的变体继承上述修复；没有新增配置键。
- 前进日志原因从 `forward_open_loop` 改为 `forward_encoder_heading_hold`。
- `GripperWidthPickupSequence.step(..., heading_rad=...)` 使用调用方的连续航向。
  未注入航向的独立测试入口仍只按编码器定距，不宣称具备航向纠偏。
- 保留本次工作前已有的配置、感知修改及 2315 重构。未提交、未推送。

## 验证与限制

新增 `tests/test_match1818_regressions.py`：5/10 ms 轮询、300/600 ms 结果延迟，
真实准备器单帧抓取到开爪/前进，以及冻结转角在缺帧时的推进、远场交接和失败后可见替代目标接管。
原有危险侵入、缺几何、真实运动、遥测失效、载物/补夹和失败区域回归继续保留。
全量 pytest：1330 通过、3 失败；失败均为既有 NB 配置与固定默认值断言不一致
（首点配置为 `(350,600)`、测试要求 `(100,800)`，另有配置整体相等断言）。
本次未覆盖用户配置或放宽这些断言。`compileall` 与 `git diff --check` 通过。

未验证：本次修复后的真车效果、Pi/Hailo 控制周期与端到端延迟、启动瞬间左右轮实际速度差。
日志按约一秒输出状态，不能证明最初右偏的硬件原因。需要同场复测绿块及橙块，记录开爪前、
开爪期间、启动后航向与高频轮速，确认实际转角减小和抓获情况；不以软件动作完成当作抓获成功。
