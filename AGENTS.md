# AGENTS.md

适用于整个仓库。项目面向 2027 工创赛智能救援；规则以最新正式文件和现场通知为准，官方资料位于 `docs/命题文件/`。

## 开始工作

1. 先运行 `git status --short`，保留用户已有修改，不清理无关文件。
2. 阅读 `README.md`、`docs/项目结构.md`，再按任务读取相邻 README 或专项文档。
3. 以实际调用方、配置和测试确认现状；规划文字不能作为已实现证据。
4. 修改前确认权威实现，避免并行增加第二套配置、坐标或规则解释。

涉及比赛规则时读 `docs/赛题约束与视觉需求.md`；涉及优先级读 `docs/后续优先级.md`；标定、数据采集、Pose、任务规则和真机验收分别读对应模块 README、`docs/数据采集工具使用.md`、`docs/Pose视觉模型约定.md`、`docs/规则状态机设计.md` 和 `manual_tests/README.md`。

## 实现原则

- 优先完成最小一致性改动；先复用现有模块，只在职责独立且有真实实现时新增目录或抽象。
- CLI 只负责参数解析与装配，可测试规则放在领域模块；不为预想复用增加工厂、注册表、适配层或兼容包装。
- 内部 API 可一次更新全部调用方时直接修改，不保留无实际用途的旧接口。
- 删除或更改公共 API、schema、配置和命令前检查调用方、文档及测试，并同步处理迁移影响。
- 文件名直接表达职责，避免无边界的 `utils.py`、`common.py`、`manager.py`。
- Python 支持 3.10+、目标 3.13；使用 `from __future__ import annotations`，公共数据优先采用明确类型和 `slots=True` dataclass。
- 时间字段标明单位；资源使用上下文管理或 `try/finally`，异常路径也应释放相机、线程、进程、连接和窗口。
- 在系统输入边界校验尺寸、值域、有限值、矩阵有效性和枚举；错误信息包含实际值。

## 架构约束

- 跨模块坐标使用 `RawPixel`、`UndistortedPixel`、`GroundPoint`、`FieldPoint`、`BevPixel` 等明确类型，不传递无语义点。
- 机器人系为 `x` 向前、`y` 向左、`z` 向上，单位 mm；像素 `u` 向右、`v` 向下；图像尺寸为 `(width, height)`，数组 shape 为 `(height, width, channels)`。
- `CameraModel` 是去畸变唯一权威；`GroundProjector` 是去畸变像素与机器人地面投影的唯一权威，不复制标定矩阵。
- 检测器输出带帧号、时间、坐标系、置信度和质量信息的观测，不直接修改定位或全局状态。
- 算法依赖注入的 `FrameSource`、推理后端和通信接口，不直接创建 Picamera2、Hailo 或串口对象。
- 类别、阈值、场地尺寸和红蓝映射来自运行配置；完整 BEV 仅按需生成并在同帧复用。
- 实时链路采用最新数据或有界队列。相机、去畸变、推理、编码、显示、写盘和网络 I/O 不得阻塞运动控制循环。
- 旁路异常应显式上报并隔离，不能默认升级为停车；控制循环依据仍可用的状态和具体风险选择继续、限速、重试、绕行或退出当前动作。

## 运动与比赛规则

- 不因单帧漏检、短暂观测过期、定位质量下降、旁路失败或 `rx_degraded` 单独停车；`rx_degraded` 保留在状态、日志和诊断中，不作为独立运动门禁或重同步触发条件。
- 降级动作应与证据和场景匹配，优先采用限速、缩短动作时域、重新观测、回退到近期可靠状态或更换路径。
- 仅在继续运动存在明确且更高的碰撞、越界、规则终止或失控风险，远程死手/明确急停触发，或执行动作所需的关键控制状态确实不可用时停车；恢复条件应明确，避免无期限保持零速。
- 停车、重新使能和恢复策略属于运动或任务层，不由感知、日志、显示、录制和通信旁路自行决定。
- 危险目标进入安全区或离场会结束本轮；相关逻辑保留未知/疑似状态，并单独关注漏检。
- 首次有效转运为单个普通物资；伤员单独转运；一次物资不超过 3 个。规则集中在状态机，不散落到检测器。
- 不增加强光、激光、电子干扰、攻击性或误导性视觉方案。

## 标定、配置与数据

- 标定绑定相机、分辨率、裁剪、焦点、安装位姿和 `new_K`；条件变化时更新记录并验证受影响链路。
- 默认拒绝 `quality.usable=false` 或元数据不匹配的标定，不静默回退。
- 配置和数据只维护当前格式；格式变化时同步更新仓库内调用方、测试和专项文档，不增加无需求的版本或兼容层。
- 不提交 `calibration_captures/`、`output/`、批量录像、原始数据集、大模型权重、密钥、个人信息或比赛匿名要求禁止的标识。
- 合成数据可验证逻辑和 schema，不能作为现场实拍、危险类指标或硬件性能证据。

## 测试与验证

- 每次先运行覆盖改动的最小测试：优先单个测试函数或测试文件，必要时再扩展到关联模块。
- 文档改动只做内容、链接和格式检查；不因纯文档改动运行全量 pytest 或 compileall。
- 新增或改变纯逻辑时补充对应的无硬件 pytest；修复缺陷时优先加入能复现问题的回归用例。
- 仅当改动跨模块、触及公共装配/配置、影响收集导入，或准备发布时运行更广测试；全量验证按需要使用 `python -m compileall -q src tests manual_tests` 和 `python -m pytest`。
- 硬件、GUI 与目标设备性能在 `manual_tests/` 或真机完成；自动测试收集时不得打开设备或窗口。
- 感知结果按相关类别报告，危险类单列；性能结论报告目标硬件端到端延迟和观测年龄。未执行的必要验证明确写“未验证”。
- 不通过删除断言、降低安全阈值或跳过危险类用例让测试通过。

## 文档与交付

- 状态变化更新 README；结构变化更新 `docs/项目结构.md`；开放任务变化更新 `docs/后续优先级.md`；规则解释变化更新 `docs/赛题约束与视觉需求.md` 并注明来源。
- schema、命令或现场流程只更新对应专项文档及必要入口；一个概念只保留一个文档权威，计划能力不得写成已完成。
- 修改公共 API、CLI、配置装配或返回契约时，更新相邻模块 README 中受影响的入口和生产用法；示例使用实际运行配置并说明资源生命周期与坐标/时间语义。
- 交付说明包含修改内容、关键决策、已执行验证、迁移影响和仍存在的风险。

## Git

- 只暂存本任务文件；提交前检查状态和 staged diff，不夹带用户或其他任务修改。
- 内聚改动通过相应验证后可提交，使用仓库现有提交前缀；验证失败的状态不提交。
- 未经用户明确授权，不执行 push、amend、rebase、reset、强制更新历史、删除分支或破坏性文件清理。
