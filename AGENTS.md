# AGENTS.md

本文件适用于整个仓库。项目维护 2027 工创赛智能救援上位机视觉软件；官方资料在 `docs/命题文件/`，但以最新正式文件和现场通知为准。

## 开始工作

1. 运行 `git status --short`，保留用户已有修改，不清理无关文件。
2. 阅读 `README.md` 和 `docs/项目结构.md`，再按下表只加载任务所需文档。
3. 查看实际调用方和测试，不根据规划文字假设模块已存在。
4. 修改前明确权威实现，避免增加第二套配置、坐标或规则解释。

| 任务 | 必读文档 |
| --- | --- |
| 比赛类别、区域、安全规则 | `docs/赛题约束与视觉需求.md`，必要时核对官方 PDF |
| 新模块、优先级、依赖 | `docs/后续优先级.md` |
| 相机或地面标定 | `src/rescue_vision/calibration/README.md` |
| 录制、检查、清单与划分 | `docs/数据采集工具使用.md`；schema 再看 `docs/数据集与评测.md` |
| 现场四类目标采集 | `docs/数据采集工具使用.md` 的“正式采集覆盖” |
| 目标标注或 Hailo Pose | `docs/Pose视觉模型约定.md`、`src/rescue_vision/perception/README.md` |
| 跟踪、世界模型或任务规则 | 对应模块 `README.md`；规则改动再看 `docs/规则状态机设计.md` |
| 真机或 GUI 验收 | `manual_tests/README.md` |

## 当前事实

- 已实现：最新帧相机源、三模型内参/地面标定、去畸变、BEV、严格配置、录制回放、数据工具、离线评测、UART/TCP 通信基础、Rescue Car 运动控制与远程调试执行、任务目标观测契约、Hailo YOLO Pose 后端，以及纯逻辑跟踪、最小世界模型和规则状态机。
- 未实现：正式任务目标模型、定位、区域/对手感知、真实接触/交付证据适配、规划、正式通信发布器和应用入口。
- `tests/` 是无 Raspberry Pi、相机和 Hailo 也能运行的 pytest；硬件/GUI 检查在 `manual_tests/`。
- 当前缺四类规则目标的系统实拍数据；`examples/` 仅是 schema 夹具。

任何文档、注释或交付说明都不得把计划模块描述为已完成。

## 结构与可读性

- 一个具体职责优先使用一个模块；不要为单个小文件创建新顶层包。
- 新目录必须同时包含真实实现和测试，不创建空架构或占位接口树。
- 文件名直接表达实现，例如 `rpicam_source.py`；避免 `utils.py`、`common.py`、`manager.py` 等无边界容器。
- CLI 保持为参数解析和领域函数调用的薄层；可测试规则放在领域模块。
- 共享抽象只在至少两个真实实现需要时提取。不要为预想中的复用提前增加工厂、注册表、适配层或基类。
- 内部 API 尚未发布且仓库内调用方可一次更新时，直接更新调用方，不保留无用途的兼容别名或包装器。
- 不为缩短文件机械拆分完整工作流。只有稳定算法被多个入口复用，或职责确实独立时才拆模块。
- 删除代码前先查调用方、CLI、文档和测试；对外 schema 或用户命令变化必须说明迁移影响。
- 每个概念只有一个文档权威：README 负责入口和状态，项目结构负责定位和边界，优先级只列开放工作，专项文档负责操作/schema。不要复制整段说明。

## 架构边界

- 坐标必须使用或明确对应 `RawPixel`、`UndistortedPixel`、`GroundPoint`、`FieldPoint`、`BevPixel`；禁止用无语义 `(u, v)` 跨模块传递。
- 机器人地面系为 `x` 向前、`y` 向左、`z` 向上，单位 mm；像素 `u` 向右、`v` 向下。
- `CameraModel` 是去畸变唯一权威；`GroundProjector` 是去畸变像素与机器人地面的唯一权威。其他模块不得复制标定矩阵。
- 检测器只产出带帧号、时间、坐标系、置信度和质量标记的观测，不直接修改定位或全局状态。
- 算法依赖 `FrameSource`、推理后端和通信接口，不直接创建 Picamera2、Hailo 或串口对象。
- 实时路径只处理最新帧；录像、显示、日志和通信使用有界旁路，不积累无界队列。
- 主运动控制循环不得执行可能造成不可控延迟的任务。循环内只允许轮速安全刷新、
  非阻塞 UART 排空、读取已完成的最新快照和轻量决策；相机等待/取帧、全尺寸
  去畸变、BEV、模型推理、图像编码、显示、写盘及可能阻塞的网络操作必须放入
  独立有界旁路，采用丢旧保新或明确背压，并把旁路异常传播到统一停车路径。
- 新增真车入口必须用测试证明慢取帧、慢去畸变或慢推理不会阻塞主运动循环，并在旁路
  失效或观测过期时保守停车。
- 少量关键点直接地面投影；完整 BEV 仅按需生成并在同帧复用。
- 类别、阈值、场地尺寸和红蓝映射必须配置化，以支持决赛现场替换。

## 规则安全

- 危险目标进入安全区或离场会结束本轮；相关改动必须单独评估漏检，并保留“未知/疑似危险”。
- 首次有效转运必须为单个普通物资；伤员必须单独；一次物资不得超过 3 个。规则集中在状态机，不散落在检测器。
- 对方安全区、场界、对手和过期视觉数据必须有保守降级策略。
- 当前上位机运动策略明确不把 STM32 的 `rx_degraded` 作为单独运动门禁或重同步触发条件；该位仍须保留在状态、日志和诊断中。除非后续先完善分级/可恢复的链路安全门禁（含连续有效帧与 ACK、错误阈值、显式重新使能以及硬件/固件看门狗验证），否则不得在后续改动中把这个门禁加回来；重新评估时必须作为新的安全改动同步更新实现、测试和专项文档。
- 不增加强光、激光、电子干扰、攻击性或误导性视觉方案。

## 标定、配置与数据

- 图像尺寸统一为 `(width, height)`；NumPy shape 为 `(height, width, channels)`。
- 标定绑定相机、分辨率、裁剪、焦点、安装位姿和 `new_K`。改变任一条件时更新记录并重新验证相应标定。
- 默认拒绝 `quality.usable=false` 或元数据不匹配的标定，不静默回退。
- 配置和数据只维护当前格式，不记录 schema 版本、Git 提交、模型/文件校验和或数据集版本；格式变化时一次更新仓库内调用方、测试和文档，不增加兼容层。标定产物继续使用可读 `calibration_id` 关联。
- 不提交 `calibration_captures/`、`output/`、批量录像、原始数据集或大模型权重。小型测试夹具要说明来源、许可和用途。
- 合成数据可以测试逻辑和 schema，但不能充当现场实拍、危险类指标或硬件性能证据。

## 实现风格

- 支持 Python 3.10+，目标为 Python 3.13；使用 `from __future__ import annotations`。
- 公共数据结构优先使用带 `slots=True` 的 dataclass 和明确类型。
- 时间字段包含单位，例如 `timestamp_ns`；坐标字段包含坐标系或使用专用类型。
- 文件和网络资源用上下文管理或 `try/finally`；相机、进程、线程和窗口在异常路径也必须关闭。
- 在入口校验矩阵形状、有限值、可逆性、尺寸、单位和枚举；错误信息包含实际值。
- 保持中文项目文档；代码标识符和对外 schema 使用稳定、清晰的英文。
- 优先完成最小一致性改动；只有维护关键不变量、删除重复实现或避免规则分散时扩大范围。

## 测试与验证

按风险运行最小充分验证，交付前至少执行：

```bash
python -m compileall -q src tests manual_tests
python -m pytest
```

新增纯逻辑必须有无硬件 pytest。硬件/GUI 脚本不能在测试收集时导入 `picamera2`、打开窗口或访问设备。

几何改动至少覆盖：

- 空输入、单点和批量；
- 像素↔地面、地面↔BEV 往返；
- BEV 四角、方向和边界；
- 错误分辨率、奇异矩阵、不可用标定；
- 与已知 OpenCV 结果的数值对照。

感知改动报告分类别指标和失败样例，危险类单列。性能改动报告目标硬件端到端延迟和观测年龄，不只报告推理 FPS。当前环境不能执行的验证必须明确写为“未验证”。

## 文档与交付

- 实现状态变化：更新 README 状态表。
- 文件位置或模块边界变化：更新 `docs/项目结构.md`。
- 开放任务、依赖或顺序变化：更新 `docs/后续优先级.md`。
- 规则解释变化：更新 `docs/赛题约束与视觉需求.md` 并注明来源；不改写官方 PDF。
- schema、命令或现场流程变化：只更新对应专项文档及 README 入口，不在多处复制细节。
- 每个已实现 Python 包维护相邻 `README.md`，定位为模块对接手册，而不是测试说明或文件名清单。
- 模块 README 至少列出常用公共类、函数和命令，说明输入输出、生命周期、坐标/时间单位、可选返回值和错误或降级语义；只列真实存在且适合调用方依赖的入口。
- 模块 README 的主要代码示例必须体现预期生产用法：凡运行配置能够装配的对象，优先从 `configs/runtime.yaml` 经 `load_runtime_config()` 获取参数和资产，不在示例中另写一套路径、阈值、类别映射或相机参数。
- 模块 README 的使用示例按实际接入顺序分段组织：先单独给出配置加载、对象装配和启用条件，再在后续小节分别展示常用函数或方法；不要把初始化、全部操作和清理挤进一个难以复用的大代码块。
- 后续示例可以承接前文已经声明的 `config`、通道、模型或领域输入，但必须在正文明确说明依赖哪个前置代码段、当前资源是否已进入上下文，以及变量由哪个上游产生；不能无说明地使用未定义名称。
- 每个常用操作优先使用独立代码段，例如读取、更新、发送、接收、停止和复位分别展示。代码段之间共享前置装配时不重复复制配置和参数，确保读者能按文档顺序组合成一致的生产调用链。
- 资源型模块必须把“创建但未打开”“进入上下文或显式启动”“正常操作”“故障/退出清理”分开说明。关闭连接、串口或相机若不等于领域安全动作，示例必须额外展示正确的停止或降级调用。
- 不把临时目录、合成数组、内联伪造记录、`FakeInferenceBackend` 或测试夹具作为主要使用示例。测试替身可以单独说明用途，但必须与正式接入示例明确分开。
- 示例应在其声明的上下文内完整、一致，并附必要注释解释“为何这样调用”、资源由谁关闭、数据处于哪个坐标系以及下游如何消费；不得只展示能运行但无法支撑核心功能对接的片段。
- 新增包或修改公共 API、CLI、配置装配或返回契约时，同步更新相邻 README 的入口表和至少一个实际使用示例，并核对导入路径、函数签名和命令参数。
- 交付说明包含修改内容、关键决策、已执行验证、迁移影响和仍存在的风险。

## Git 提交

- Agent 可以在一个内聚改动完成并通过相应验证后自行及时提交，无需逐次征求许可。
- 提交按可独立理解、验证和回退的功能边界组织；不要把所有改动挤进一个大提交，也不要为每个小文件制造碎片提交。
- 提交前检查 `git status --short` 和 staged diff，只暂存本任务产生或用户明确要求纳入的文件；用户原有及无关修改不得顺带提交。
- 使用明确的路径或交互式暂存控制范围。提交信息简洁说明意图，沿用仓库现有的 `feat:`、`fix:`、`docs:`、`refactor:` 等前缀。
- 代码提交应包含对应测试；外部行为、结构或命令变化应在同一提交中包含必要的相邻文档。
- 验证失败时先修复或明确保留未提交，不提交明知失败的状态。
- 自主提交权限不包含 `push`、`commit --amend`、rebase、reset、强制更新历史或删除用户分支；这些操作仍需用户明确授权。

## 禁止事项

- 不提交密钥、个人信息或比赛匿名文档中的队名/校名等禁用标识。
- 不把绝对标定路径、个人主目录或单次采集目录硬编码进运行代码。
- 不绕过标定质量检查，不混用原始像素和去畸变像素。
- 不为让测试变绿而删除断言、降低安全阈值或跳过危险类用例。
- 不执行破坏性 Git 或文件清理，除非用户明确要求并已核对目标。
